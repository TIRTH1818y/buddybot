import 'package:flutter/material.dart';

class chat_his extends StatelessWidget {
  const chat_his({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text("hello this is a history"),
    );
  }
}
